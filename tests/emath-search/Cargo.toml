[package]
name = "emath-search-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
emath-search = { path = "../../crates/emath-search" }

# Dual seal (ra96/U010): inherit the workspace lint set, including
# forbid(unsafe_code), over the test package.
[lints]
workspace = true
