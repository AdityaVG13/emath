[package]
name = "emath-provenance-tests"
version = "0.1.0"
edition = "2024"
publish = false

[features]
# Mirrors the production crate's `graphdb` gate: the engine-backed store
# tests only compile when this feature is enabled.
graphdb = ["emath-provenance/graphdb"]

[dependencies]
emath-provenance = { path = "../../crates/emath-provenance" }
