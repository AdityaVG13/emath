//! Integration tests for `emath-lsp`: lab runtime, server state machine,
//! and async transport (migrated from inline `#[cfg(test)]` modules in
//! `crates/emath-lsp/src/`).

#![forbid(unsafe_code)]
