//! Integration tests for `emath-cli`'s `lsp` module: lab runtime, server state machine,
//! and async transport (migrated from inline `#[cfg(test)]` modules in
//! `crates/emath-cli/src/lsp/`).

#![forbid(unsafe_code)]
