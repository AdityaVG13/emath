[package]
name = "emath-lsp-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
emath-lsp = { path = "../../crates/emath-lsp", features = ["async-runtime"] }
asupersync = { workspace = true }
