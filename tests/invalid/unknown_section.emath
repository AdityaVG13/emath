# expect: E-SEC-101
# `state:` is a historical section, not a constructor section.
emath function UnknownSection:
    inputs:
        x: Float64
    outputs:
        y: Float64
    state:
        s: Float64
    definitions:
        y = x
