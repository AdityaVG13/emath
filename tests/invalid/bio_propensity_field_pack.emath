# expect: E-SYN-101 field-pack propensity (named bio fence, never generic drop)
emath function bio_propensity_fence:
    inputs:
        alpha: Float64

    outputs:
        y: Float64

    definitions:
        propensity Birth: alpha * 1.0
        y = alpha
