# expect: E-SYN-101 dangling edge has no target
# Negative control an edge with no target
#  operand. Edge grammar is binary — a dangling `-->` refuses at parse
#  time instead of silently producing a half edge.
emath function f:
    definitions:
        g = graph { 1, 2; 1 --> }
