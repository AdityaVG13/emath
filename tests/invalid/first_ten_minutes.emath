# expect: E-TYPE-002 — a first-minute form that requires a hidden default is
# refused: bare `sin` would need the compiler to guess
# "treat the function as sin(x)" and `over 0..6.28` would need an ambient
# range. The admitted spelling is `plot sin(x)` (see
# tests/fixtures/language/intro/first-ten-minutes.emath).
plot sin over 0..6.28
