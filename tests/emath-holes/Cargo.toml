[package]
name = "emath-holes-tests"
version = "0.1.0"
edition = "2024"
publish = false

[[test]]
name = "intent_completion_solve_unit"
path = "intent_completion_solve_unit.rs"

[dependencies]
emath-holes = { path = "../../crates/emath-holes" }
emath-calibration = { path = "../../crates/emath-calibration" }
emath-term = { path = "../../crates/emath-term" }
emath-world-ir = { path = "../../crates/emath-world-ir" }
emath-syntax = { path = "../../crates/emath-syntax" }
emath-wasm = { path = "../../crates/emath-wasm" }

# Dual seal (ra96/U010): inherit the workspace lint set, including
# forbid(unsafe_code), over the test package.
[lints]
workspace = true
