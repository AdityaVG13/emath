[package]
name = "emath-rust-ir-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
emath-rust-ir = { path = "../../crates/emath-rust-ir" }

# Dual seal (ra96/U010): inherit the workspace lint set, including
# forbid(unsafe_code), over the test package.
[lints]
workspace = true
