# Imported custom-kind schema. `capability` is not a lexer keyword or a
# stable-IR operation enum.
use std.kinds.capability

emath capability Softmax:
    inputs:
        x: Float64
    outputs:
        probability: Float64
    definitions:
        probability = x
