# A family expands through an imported schema into ordinary capability cells.
# `family` and the operation names are not lexer keywords.
use std.kinds.family

emath family ElementwiseUnary<Op>:
    inputs:
        x: Float64
    outputs:
        value: Float64
    definitions:
        value = x
    instances:
        "sin"
        "exp"
        "sqrt"
