[package]
name = "demo-host-independent"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# The generated crate is a committed artifact (byte-identical to the
# pipeline output; `scripts/validate.sh` re-generates and diffs it).
affine-policy-rs = { path = "../generated/affine-policy-rs" }

[lints]
workspace = true
