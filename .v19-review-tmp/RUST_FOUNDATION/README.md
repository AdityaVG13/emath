# Reference Rust Foundation

These types illustrate Feature Capsules, projection closure, the Meaning Spine, and agent Context
Capsules. Existing production crates should own these responsibilities where practical.

```console
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo run -p emath-v19-demo
```
