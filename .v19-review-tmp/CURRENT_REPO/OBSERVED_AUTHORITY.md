# Observed Authority and Process

At the pinned repository state:

```text
implementation/CONSTITUTION.md:
    project laws and code enforcement anchors

implementation/VISION.md:
    router doctrine and syntax-pack decision

language/reference/:
    normative user-language prose

language/grammar/surface.ebnf:
    machine-readable secondary surface model

language/CAPABILITY.md:
    manual current behavior matrix

elps/:
    proposals, G4 gates, experimental lane, and four-artifact enforcement

language/stdlib object packs:
    executable semantic objects for selected standard-library work

compiler tables and Rust:
    current realized behavior
```

V19 adds one feature-scoped authority relation over these layers.
