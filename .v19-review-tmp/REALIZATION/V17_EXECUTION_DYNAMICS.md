# V17 Realization

```text
campaign:
    Kind Capsule lowering to existing ExperimentManifest/lab infrastructure

workload, target, schedule, metric:
    kinds or schema records

fusion, tiling, reuse, quantization:
    transformation capabilities/families

analytic/measured/quality:
    worlds

runner/compiler/profiler:
    providers

Pareto archive and receipts:
    existing artifacts
```

Hardware and runtime brands remain provider metadata, never stable language identity.
