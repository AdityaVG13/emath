# V16 Realization

Every one of the 1,224 catalog entries stays `catalog-only` until classified.

Promotion:

```text
catalog entry
→ primary feature class
→ FeatureID
→ dependencies
→ capsule
→ positive/negative/mutation conformance
→ reference/provider disposition
→ owner acceptance
→ implementation coverage
→ stable or diagnostic status
```

A mathematical field usually becomes a field pack plus many feature capsules, not one keyword.
