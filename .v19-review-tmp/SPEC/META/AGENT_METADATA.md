# Agent Metadata

Every capsule supplies:

```text
owner
minimum read set
allowed edit categories
hazards
required gates
completion condition
```

This metadata generates task context; it does not weaken semantic or repository ownership rules.
