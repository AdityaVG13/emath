# Feature Capsule Contract

Required fields:

```text
identity, class, status, edition, summary, dependencies
surface, canonical, semantics, exactness, effects, worlds
reference, artifact, projections, diagnostics, conformance
evolution, agent
```

Class-specific schemas tighten these fields. Unknown fields are refused unless the class explicitly
allows extension metadata.
