# Resource Economy

Track:

```text
handwritten core LOC per feature
duplicate semantic statements
agent orientation tokens
files and source tokens read
tool calls to first useful gate
failed edits and rework
generated-to-handwritten ratio
stale-view incidence
```

The goal is the smallest authority-complete context, not under-reading.
