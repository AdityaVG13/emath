# Meaning Spine

Typed edges:

```text
requires
surface-expands-to
lowers-to
implements
interprets
solves
provides
emits
checks
migrates-to
documents
tests
```

Derived closures:

```text
load closure
reverse impact
projection closure
conformance closure
migration closure
agent read/edit closure
tree-shaking closure
```

Ordinary dependency cycles are rejected. A deliberately recursive theory cluster needs a separate
stratification contract.
