# Semantic Transaction

A change is atomic across:

```text
specification
identity
implementation
conformance
generated projections
authority
migration
receipt
```

If any required element fails, authority remains on the last passing feature version. Partial work
may exist in a branch but cannot become stable language meaning.
