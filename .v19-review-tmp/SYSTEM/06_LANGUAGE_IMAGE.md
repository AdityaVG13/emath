# Language Image

Partitions:

```text
header and identity domains
FeatureID table
symbol trie and precedence
kind/section schemas
lowering programs
type/constraint programs
reference programs
theory/instance graph
world/method/provider manifests
diagnostics/routes
migrations
conformance index
documentation/tooling offsets
agent index
```

Desktop builds may memory-map it. WASM may load verified chunks. Every compiled artifact records the
ImageID.
