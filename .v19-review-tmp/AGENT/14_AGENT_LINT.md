# Agent Lint

Block completion when generated files were edited, FeatureID is absent, identity classification is
missing, a golden changes without authority, a blocking hole is hidden, a negative control is
absent, or indexes/receipt are stale.
