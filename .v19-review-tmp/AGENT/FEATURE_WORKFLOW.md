# Feature Workflow

```text
locate FeatureID
→ orient
→ inspect authority and holes
→ write Change Contract
→ add/adjust conformance first
→ implement
→ run targeted projection closure
→ run mutations/differentials
→ run generated-view freshness
→ emit Change Receipt
```

Stop and create a Spec Hole whenever two meanings remain plausible.
