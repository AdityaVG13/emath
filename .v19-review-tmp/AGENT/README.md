# Agent-Native Control Plane

The agent plane is generated from the Meaning Spine.

An agent should not have to guess authority, recursively inspect the repository, infer test owners,
or depend on chat history.

```text
AGENT_START
→ Task Capsule
→ generated Context Capsule
→ targeted edits
→ targeted/closure gates
→ Change Receipt
→ regenerated indexes
```
