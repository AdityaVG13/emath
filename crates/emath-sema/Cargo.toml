[package]
name = "emath-sema"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
emath-core = { workspace = true }
emath-ir = { workspace = true }
emath-exec-ir = { workspace = true }
emath-schema = { workspace = true }

[dev-dependencies]
# The session tests install the syntax parser backend once per process
# (dev-only; the kernel seam stays untouched in production builds).
emath-syntax = { workspace = true }

[lints]
workspace = true
