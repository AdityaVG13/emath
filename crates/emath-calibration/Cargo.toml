[package]
name = "emath-calibration"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Semantic calibration: example partitions, fitting, held-out challenge, drift, world versioning."

[dependencies]
emath-term = { path = "../emath-term" }
emath-world-ir = { path = "../emath-world-ir" }

[lints]
workspace = true
