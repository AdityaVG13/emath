[package]
name = "emath-hir"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Intent-compiler HIR: HirPackage, content-derived declaration IDs, IntentGraph over declarations, goals, and definitions."

[dependencies]
emath-core = { path = "../emath-core" }
emath-syntax = { path = "../emath-syntax" }
