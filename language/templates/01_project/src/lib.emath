emath custom <Square> as function:
    inputs:
        x: Float64

    outputs:
        y: Float64

    definitions:
        y = x * x

    requests:
        evaluate <y>:
            produce rust.library

    compile:
        target rust
        profile library
        numeric strict-f64
